# TODO: pull per-session weather telemetry and lap-by-lap timing/position
# data from FastF1 (~2018+ coverage) for the prediction model's weather
# features and the historical race replay feature. Cache FastF1's own
# session cache under data/cache/ (see backend/.gitignore) — never fetch
# at request time from the API layer.
