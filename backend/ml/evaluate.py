# TODO: compute the accuracy-tracker metrics documented in
# docs/api-contract.md — mean_absolute_position_error, dnf_brier_score,
# rank_correlation (Spearman's rho, DNFs placed last), and podium_hit_rate
# (|predicted_top_3 ∩ actual_top_3| / 3) — for both the per-race and
# season-level accuracy endpoints.
