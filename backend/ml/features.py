# TODO: build the feature table consumed by both models — grid_position,
# weather_category, and the derived_features documented in the prediction
# endpoint's `inputs` block (constructor_recent_form, constructor_reliability,
# circuit_overtaking_difficulty, driver_circuit_history,
# driver_historical_dnf_rate) plus an era-aware feature for the 2022
# regulation reset. Keep feature names in sync with docs/api-contract.md —
# that's the seam the v1.1 what-if mode depends on staying stable.
