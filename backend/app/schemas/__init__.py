# Pydantic request/response models go here, mirroring the JSON shapes
# documented in docs/api-contract.md. Not designed yet — router stubs
# in app/api/v1/ currently return 501 rather than a typed response.
